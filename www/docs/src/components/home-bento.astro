<section class="features">
	<div class="bento-grid" data-bento-grid>
		<div class="bento-item bento-dx" data-status="[NORMAL] README.md 10:24">
			<div>
				<span class="badge">DEVELOPER_EXPERIENCE</span>
				<h3>Developer experience.</h3>
				<p>
					Consistent tooling across the monorepo: Turborepo, pnpm workspaces,
					ESLint, and automated releases.
				</p>
			</div>
			<div class="bento-graphic mono">
				<span>import &#123; ai &#125; from "@stephansama/ai-commit-msg";</span>
				<br />
				<span>ai.generate(&#123; staged: true &#125;);</span>
			</div>
		</div>

		<div class="bento-item bento-modular" data-status="[INSERT] tree.pkg 42:12">
			<div>
				<span class="badge">COMPOSABILITY</span>
				<h3>Modular &amp; composable.</h3>
				<p>
					Packages are small and focused. Use only what you need — each package
					is independently published.
				</p>
			</div>
			<div class="bento-graphic">
				<svg
					aria-hidden="true"
					fill="none"
					height="180"
					stroke="var(--home-accent)"
					stroke-width="0.7"
					viewBox="0 0 100 100"
					width="180"
				>
					<path d="M50 10 L90 30 L90 70 L50 90 L10 70 L10 30 Z"></path>
					<path d="M50 10 L50 50 L90 30 M50 50 L10 30 M50 50 L50 90"></path>
					<circle cx="50" cy="50" fill="var(--home-accent)" r="3"></circle>
				</svg>
			</div>
		</div>

		<div class="bento-item bento-oss" data-status="[VISUAL] git_branch 0:0">
			<div>
				<span class="badge">OPEN_SOURCE</span>
				<h3>Open source.</h3>
				<p>
					All packages are open source on
					<a href="https://github.com/stephansama/packages">GitHub</a>. Built in
					the open, battle-tested by use.
				</p>
			</div>
		</div>

		<div class="bento-item bento-ts" data-status="[NORMAL] main.ts 100:100">
			<div>
				<span class="badge">TYPESCRIPT</span>
				<h3>TypeScript-first.</h3>
				<p>
					Every package is written in TypeScript with full type definitions and
					generated API documentation.
				</p>
			</div>
			<div class="bento-graphic mono">
				<span>type Package&lt;T&gt; = Readonly&lt;T&gt;;</span>
			</div>
		</div>
	</div>
</section>

<style is:global>
	.home-root .features {
		padding-bottom: var(--home-space-section);
	}

	.home-root .bento-grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		grid-template-rows: repeat(2, 320px);
		gap: 24px;
		position: relative;
		font-family: var(--home-font-display);
	}

	.home-root .bento-item {
		background: var(--sl-color-black);
		border: 1px solid var(--sl-color-gray-5);
		border-radius: var(--home-radius-sm);
		padding: 40px;
		position: relative;
		overflow: hidden;
		display: flex;
		flex-direction: column;
		justify-content: space-between;
		transition: border-color 0.3s ease;
	}

	.home-root .bento-item::before {
		content: "";
		position: absolute;
		inset: -1px;
		pointer-events: none;
		background: radial-gradient(
			300px circle at var(--mouse-x, 50%) var(--mouse-y, 50%),
			color-mix(in srgb, var(--home-accent) 22%, transparent),
			transparent 70%
		);
		opacity: var(--spotlight-opacity, 0);
		transition: opacity 0.6s cubic-bezier(0.16, 1, 0.3, 1);
		z-index: 0;
	}

	.home-root .bento-item:hover {
		border-color: var(--home-accent);
	}

	.home-root .bento-item::after {
		content: attr(data-status);
		position: absolute;
		bottom: 0;
		left: 0;
		right: 0;
		background: var(--sl-color-bg);
		border-top: 1px solid var(--sl-color-gray-5);
		padding: 6px 12px;
		font-family: var(--home-font-mono);
		font-size: 10px;
		color: var(--sl-color-gray-3);
		display: flex;
		justify-content: flex-end;
		opacity: 0.6;
		transition: all 0.3s ease;
		z-index: 2;
	}

	.home-root .bento-item:hover::after {
		opacity: 1;
		color: var(--home-accent);
		background: color-mix(in srgb, var(--home-accent) 5%, var(--sl-color-bg));
	}

	.home-root .bento-item > * {
		position: relative;
		z-index: 1;
	}

	.bento-item.bento-modular {
		margin: 0;
	}

	.home-root .bento-item h3 {
		margin: 0 0 16px;
		font-size: 22px;
		font-weight: 600;
		letter-spacing: -0.01em;
		color: var(--sl-color-white);
		font-family: var(--home-font-display);
	}

	.home-root .bento-item p {
		margin: 0;
		color: var(--sl-color-gray-3);
		font-size: 15px;
		max-width: 320px;
		line-height: 1.5;
	}

	.home-root .bento-item p a {
		color: var(--home-accent);
		text-decoration: underline;
	}

	.home-root .bento-item .badge {
		display: inline-flex;
		align-items: center;
		padding: 4px 12px;
		background: var(--sl-color-bg);
		border: 1px solid var(--sl-color-gray-5);
		border-radius: var(--home-radius-sm);
		font-size: 11px;
		font-family: var(--home-font-mono);
		color: var(--home-accent);
		margin-bottom: 16px;
		text-transform: uppercase;
		letter-spacing: 0.1em;
	}

	.home-root .bento-dx {
		grid-column: span 2;
	}
	.home-root .bento-modular {
		grid-row: span 2;
	}

	.home-root .bento-graphic {
		position: absolute;
		bottom: 40px;
		right: 40px;
		opacity: 0.1;
		transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
		z-index: 0;
		pointer-events: none;
		max-width: 60%;
	}

	.home-root .bento-graphic.mono {
		font-family: var(--home-font-mono);
		font-size: 11px;
		color: var(--sl-color-gray-3);
		text-align: right;
		line-height: 1.6;
	}

	.home-root .bento-item:hover .bento-graphic {
		opacity: 0.3;
		transform: scale(1.05) rotate(2deg);
	}

	@media (max-width: 1024px) {
		.home-root .bento-grid {
			grid-template-columns: 1fr;
			grid-template-rows: auto;
		}
		.home-root .bento-dx,
		.home-root .bento-modular {
			grid-column: span 1;
			grid-row: span 1;
		}
		.home-root .bento-item {
			height: 320px;
		}
	}
</style>

<script>
	const prefersReducedMotion = window.matchMedia(
		"(prefers-reduced-motion: reduce)",
	).matches;
	const grid = document.querySelector<HTMLElement>("[data-bento-grid]");

	if (grid && !prefersReducedMotion) {
		const items = grid.querySelectorAll<HTMLElement>(".bento-item");
		for (const item of items) {
			item.addEventListener("mousemove", (event) => {
				const rect = item.getBoundingClientRect();
				item.style.setProperty("--mouse-x", `${event.clientX - rect.left}px`);
				item.style.setProperty("--mouse-y", `${event.clientY - rect.top}px`);
			});

			item.addEventListener("mouseenter", () => {
				item.style.setProperty("--spotlight-opacity", "1");
			});

			item.addEventListener("mouseleave", () => {
				item.style.setProperty("--spotlight-opacity", "0");
			});
		}
	}
</script>
